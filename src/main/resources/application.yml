spring:
  application:
    name: actuator

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
  endpoint:
    health:
      show-details: always

app:
  server:
    prop: Hello World!